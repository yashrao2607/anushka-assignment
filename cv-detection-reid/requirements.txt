# ---- Core: everything Phase 1-3 needs to run today -------------------------
PyYAML>=6.0                 # typed config loading
numpy>=1.26
opencv-python>=4.9          # video I/O, GMC optical flow, drawing, writing
torch>=2.5                  # CPU wheel is fine; CUDA wheel for training
torchvision>=0.20           # ReID fallback backbone (ResNet18)
ultralytics>=8.3            # YOLO11 training / validation / export
scipy>=1.11                 # Hungarian assignment (linear_sum_assignment)
matplotlib>=3.8             # calibration curve, PR curves, Pareto plot
pytest>=8.0
pytest-cov>=5.0

# ---- Optional: better ReID backbone (PRD 9.2 specifies OSNet) --------------
# torchreid>=1.4            # pip install torchreid  -> enables osnet_x0_25
# The pipeline runs without it and reports which backbone produced each number.

# ---- Optional: export arm (EXP-8, NFR-6) -----------------------------------
onnx>=1.16
onnxruntime>=1.18
# tensorrt                  # GPU only; documented rather than executed (R9)

# ---- Optional: UI ----------------------------------------------------------
streamlit>=1.38
