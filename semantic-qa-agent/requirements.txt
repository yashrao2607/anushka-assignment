# ---------------------------------------------------------------------------
# Semantic Search / Intelligent Q&A Agent -- full dependency set.
# Install with:  pip install -r requirements.txt
# ---------------------------------------------------------------------------

# Core / Phase 1 -- ingestion
PyYAML>=6.0                 # typed config loading
pymupdf>=1.24               # PDF parsing (fast, per-page, layout-aware)
python-docx>=1.1            # DOCX parsing
beautifulsoup4>=4.12        # HTML parsing (regex fallback if absent)
tiktoken>=0.7               # exact token counting (approximation if absent)

# Phase 2 -- retrieval
sentence-transformers>=3.0  # bi-encoder embeddings AND the cross-encoder
chromadb>=0.5               # persistent vector store (numpy fallback if absent)
rank-bm25>=0.2.2            # Okapi BM25 sparse retrieval
numpy>=1.26

# Phase 3 -- generation + UI
requests>=2.31              # Groq REST client (no SDK dependency)
streamlit>=1.38             # web UI

# Development
pytest>=8.0
pytest-cov>=5.0
